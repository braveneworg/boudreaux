# boudreaux — Claude Code

All development guidelines for this repo — stack, architecture, project
structure, commands, TypeScript/React conventions, testing, performance,
naming, security, commit rules, and the two hard constraints (E2E database
isolation and secrets/`.env*` handling) — live in **[AGENTS.md](./AGENTS.md)**,
the single source of truth shared with every coding agent. The `@` import below
pulls its full content into context; read and follow it exactly.

@AGENTS.md

## Claude Code–specific notes

_None beyond AGENTS.md today._ Add Claude-only guidance here (e.g. plan-mode
requirements for sensitive paths, or preferred subagents) so it augments
AGENTS.md without duplicating or forking it.
