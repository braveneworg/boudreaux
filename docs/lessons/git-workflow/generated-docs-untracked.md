# Generated architecture docs stay untracked

`docs/architectural-diagrams/` is intentionally gitignored (2026-07-21):
generated architecture docs and PDFs stay on the machine that renders them
and are rebuilt with its `build-pdf.js` — never re-commit them, even though
pre-2026-07 history did.
