apiVersion: 1

datasources:
  - name: Loki
    type: loki
    # Stable uid so provisioned alert rules and dashboards can reference it
    uid: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: false
    jsonData:
      maxLines: 1000
