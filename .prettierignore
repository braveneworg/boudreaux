# Dependencies
node_modules
.pnp
.pnp.js

# Testing
coverage

# Next.js
.next/
out/
build
dist

# Production
.vercel

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env
.env*.local

# Lock files
package-lock.json
yarn.lock
pnpm-lock.yaml

# Auto-generated release notes (semantic-release rewrites this on every release)
CHANGELOG.md

# Prisma
prisma/migrations

# Public assets
public/

# CI / infra configs — these conventionally use double-quoted YAML (GitHub
# Actions, Docker Compose, Codecov). Leave them to their own quoting style so
# Prettier (e.g. format-on-save) doesn't churn quotes on every edit.
.github/
docker-compose*.yml
codecov.yml

# SDD harness scratch files (git-ignored agent briefs/reports; only the
# repo-root .gitignore is read by Prettier, so ignore them explicitly)
.superpowers/
